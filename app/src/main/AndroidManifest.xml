<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masquilierpemeja.rugbyfantasytop14">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Signup.SignupActivity" />
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MotDePasseOublie.MotDePasseOublieActivity" />
        <activity android:name=".CreerChampionnat.CreerChampionnatActivity" />
        <activity android:name=".RejoindreChampionnat.RejoindreChampionnatActivity" />
        <activity android:name=".MenuPrincipal.MenuPrincipalActivity" />
        <activity android:name=".Profil.ProfilActivity" />
        <activity
            android:name=".BottomNavigation.BottomNavigationActivity"
            android:label="@string/title_activity_bottom_navigation">


        </activity>
    </application>

</manifest>